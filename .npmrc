# Conflicto de peers pre-existente: @nx/angular@20 espera
# @angular-devkit/build-angular 17-19, pero el repo lo aliasa a @angular/build@20.
# Sin esto, `npm install` a secas falla con ERESOLVE y deja el arbol incompleto
# (se cae @angular-devkit/core y nx serve no arranca).
legacy-peer-deps=true
